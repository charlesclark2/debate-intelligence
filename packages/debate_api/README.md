# debate_api

FastAPI service (V2) exposing `debate_core` use cases over REST/JSON with Cognito JWT auth.
Routes only — **no business logic here**. First spec:
`plan_specs/v2/e12-identity-api/t02-fastapi-service.yaml`.
