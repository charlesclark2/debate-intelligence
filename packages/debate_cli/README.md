# debate_cli

Typer + Rich commands for the `debate-research` CLI (V1). Commands parse arguments and call
`debate_core` application services — **no business logic here**. First spec:
`plan_specs/v1/e01-repo-foundation/t07-cli-skeleton.yaml`.
